<p id="notice"><%= notice %></p>

<p>
  <strong>Content:</strong>
  <%= @comment.content %>
</p>

<p>
  <strong>User:</strong>
  <%= @comment.user_id %>
</p>

<p>
  <strong>Micropost:</strong>
  <%= @comment.micropost_id %>
</p>

<%= link_to 'Edit', edit_micropost_comment_path( @micropost.id, @comment.id ) %> |
<%= link_to 'Back', micropost_path(@micropost.id) %>
